-- Create tables for Final Project
CREATE TABLE sample(
	GENDER VARCHAR(6) NOT NULL,
	AGE INT NOT NULL,
	SMOKING VARCHAR(8) NOT NULL,
	ALCOHOL VARCHAR(8) NOT NULL,
	LUNG_CANCER VARCHAR(20) NOT NULL
);

SELECT * FROM sample;